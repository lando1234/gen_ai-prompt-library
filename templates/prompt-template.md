---
type: [meta-prompt | system-design | code-generation | analysis | strategic | workflow | specialized]
complexity: [basic | intermediate | advanced | expert]
context: [technical | business | hybrid]
output_format: [structured | free-form | code | markdown]
interaction_mode: [single-shot | iterative | conversational]
production_ready: [yes | experimental]
tags: [optional, additional, keywords]
---

# [Verb] [Object] [Context/Qualifier]

## Usage

Describe **when** and **why** this prompt should be used.

Include:
- **Context**: What problem does this solve?
- **Goals**: What is the desired outcome?
- **Constraints**: When should this NOT be used?
- **Prerequisites**: What must be true before using this prompt?

Be specific. Avoid vague statements like "useful for various scenarios."

---

## Inputs

List all required inputs explicitly. No implicit assumptions.

Format as a table or list:

| Input | Type | Description | Example |
|-------|------|-------------|---------|
| `variable_name` | string | What this represents | `"example value"` |
| `another_input` | JSON object | Structure and purpose | `{"key": "value"}` |

**Optional Inputs** (if any):
- `optional_var`: Description and default behavior if not provided

---

## Outputs

Define the expected output format with precision.

Include:
- **Format**: JSON, Markdown, code, plain text, etc.
- **Structure**: Schema or example
- **Success Criteria**: How to verify the output is correct
- **Failure Modes**: What could go wrong and how to detect it

**Example Output:**
```
[Show a realistic example of what the output looks like]
```

---

## Prompt

[Insert the full, copy-pasteable prompt here]

This section should:
- Be a single, uninterrupted block
- Include all instructions and context
- Use placeholder syntax like `{variable_name}` for inputs
- Be immediately usable by copying and replacing placeholders

---

## Notes

*(Optional section for additional context)*

- Edge cases to be aware of
- Known limitations
- Variations or adaptations
- Related prompts in this library
